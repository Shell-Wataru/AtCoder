#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>
#include <queue>
#include <set>
#include <ctime>
#include <map>
#include <boost/multiprecision/cpp_int.hpp>
#include <string>

using namespace std;
using ll = long long;
namespace mp = boost::multiprecision;

int main()
{
    ll N;
    string S;
    cin >> S;
    ll red_positive_sum;
    ll red_negative_sum;
    ll blue_positive_sum;
    ll blue_negative_sum;
    for (size_t i = 0; i < N; i++)
    {
        ll A
    }
    return 0;
}