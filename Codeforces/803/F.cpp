#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>
#include <queue>
#include <set>
#include <map>
#include <iomanip>
#include <cmath>

using namespace std;
using ll = long long;

ll sort_array(vector<ll> &a){
  ll n = a.size();
  for(int i = 0;i < n;i++){
    for(int j = 0)
  }
}

int solve()
{

  ll n,k;
  scanf("%lld", &n);
  vector<ll> a(n);
  for(int i = 0;i < n;i++){
    cin >> a[i];
  }
  for(int i = 0;i < n;i++){
    cin >> b[i];
  }
  return 0;
}
int main()
{
  ios_base::sync_with_stdio(0); cin.tie(0);
  int t;
  cin >> t;
  for (size_t i = 0; i < t; i++)
  {
  solve();
  }
  cout << flush;
  return 0;
}