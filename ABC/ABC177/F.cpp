#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>
#include <queue>
#include <set>
#include <map>
#include <limits>
#include <cmath>
#include <iomanip>
#include <functional>
#include <random>
#include <set>
using namespace std;
using ll = long long;

using namespace std;

int main()
{
    // 整数の入力
    ll H,W;
    cin >> H >> W;
    ll min_path = 0;
    vector<ll> path_froms(W);
    for(int i = 0;i < W;i++){
        path_froms[i] = i;
    }
    for(int i = 0;i < H;i++){
        ll a,b;
        cin
    }
    return 0;
}