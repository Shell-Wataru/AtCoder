#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>
#include <queue>
#include <set>
#include <map>
#include <iomanip>
#include <cmath>

using namespace std;
using ll = long long;

int main()
{
    // 整数の入力
    long long n;
    map<pair<ll,ll>,ll> pairs;
    cin >> n;
    for(int i = 0;i < n;i++){
        ll a,b;
        cin >> a >> b;
        pairs[make_pair(a,b)];
    }
    sort(pairs.begin(),pairs.end());
    for(int i = 0;i<)
   return 0;
}