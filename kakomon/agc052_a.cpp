#include <iostream>
#include <algorithm>
#include <vector>
#include <deque>
#include <queue>
#include <set>
#include <map>
#include <limits>
#include <cmath>
#include <iomanip>
#include <functional>
#include <random>
#include <boost/multiprecision/cpp_int.hpp>

namespace mp = boost::multiprecision;
using namespace std;

using ll = long long;

int solve()
{
    ll N;
    string S1, S2, S3;
    cin >> N;
    cin >> S1;
    cin >> S2;
    cin >> S3;
    cout << string(N, '1') << string(N, '0') << 1 << endl;
    return 0;
}
int main()
{
    ll T;
    cin >> T;
    while (T--)
    {
        solve();
    }

    return 0;
}